# Testing Template - Unit + E2E

**Purpose**: Template para testes (Rust + TypeScript)

---

## 🧪 Rust Unit Tests (Substrate)

```rust
use crate::{mock::*, Error, Event};
use frame_support::{assert_noop, assert_ok};

#[test]
fn test_happy_path() {
    new_test_ext().execute_with(|| {
        assert_ok!(Pallet::extrinsic(RuntimeOrigin::signed(1), 42));
        
        System::assert_last_event(Event::Success { value: 42 }.into());
    });
}

#[test]
fn test_error_case() {
    new_test_ext().execute_with(|| {
        assert_noop!(
            Pallet::extrinsic(RuntimeOrigin::signed(1), u32::MAX),
            Error::<Test>::Overflow
        );
    });
}
```

---

## ✅ TypeScript Unit Tests (Jest)

```typescript
describe('Service', () => {
  let service: Service;

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [Service],
    }).compile();

    service = module.get(Service);
  });

  it('should work', async () => {
    const result = await service.method();
    expect(result).toBeDefined();
  });
});
```

---

## 🔗 E2E Tests (NestJS)

```typescript
describe('AppController (e2e)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleFixture = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
  });

  it('/endpoint (POST)', () => {
    return request(app.getHttpServer())
      .post('/endpoint')
      .send({ data: 'test' })
      .expect(201);
  });

  afterAll(async () => {
    await app.close();
  });
});
```

---

**Generated by**: Claude Code | **Version**: 1.0.0
